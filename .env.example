# GitHub Personal Access Token with 'repo' scope
GITHUB_TOKEN=your_github_token_here

# Repository owner (defaults to stb13579 if not set)
REPO_OWNER=stb13579

# Repository name (defaults to traffic-monitor if not set)
REPO_NAME=traffic-monitor

# CSV output file (defaults to traffic-data.csv if not set)
CSV_FILE=traffic-data.csv
